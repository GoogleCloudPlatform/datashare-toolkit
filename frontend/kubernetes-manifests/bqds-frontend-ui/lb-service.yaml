---
apiVersion: v1
kind: Service
metadata:
  name: bqds-frontend-ui-lb
  labels:
    app: bqds-frontend-ui
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      name: http
      targetPort: 8080
  selector:
    app: bqds-frontend-ui
    version: v1
