---
apiVersion: v1
kind: Service
metadata:
  name: bqds-frontend-ui
  labels:
    app: bqds-frontend-ui
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: http
  selector:
    app: bqds-frontend-ui
    version: v1
