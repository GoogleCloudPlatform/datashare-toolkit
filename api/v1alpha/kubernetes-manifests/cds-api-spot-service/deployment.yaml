---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: cds-api-spot-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cds-api-spot-service
        version: v1
    spec:
      volumes:
      - name: google-cloud-key
        secret:
          secretName: cds-api-spot-service-creds
      containers:
        - name: cds-api-spot-service
          image: gcr.io/chrispage-dev/cds-api-spot-service
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5555
          volumeMounts:
          - name: google-cloud-key
            mountPath: /var/secrets/google
          env:
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: /var/secrets/google/key.json
          - name: FULFILLMENT_CONFIG_BUCKET_NAME
            valueFrom:
              configMapKeyRef:
                name: environment-config
                key: FULFILLMENT_CONFIG_BUCKET_NAME
