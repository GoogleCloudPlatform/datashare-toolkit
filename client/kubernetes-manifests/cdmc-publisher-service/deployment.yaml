---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: cdmc-publisher-service
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: cdmc-publisher-service
        version: v1
    spec:
      volumes:
      - name: google-cloud-key
        secret:
          secretName: cdmc-service-creds
      containers:
        - name: cdmc-service
          image: gcr.io/chrispage-dev/cdmc
          args: ["multicast", "publish", "-p", "$(PROJECT_ID)", "-t", "$(TOPIC_NAME)", "-a", "$(ADDRESS)", "-i", "$(IF_NAME)", "-v=$(VERBOSE)"]
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50000
          volumeMounts:
          - name: google-cloud-key
            mountPath: /var/secrets/google
          env:
          - name: GOOGLE_APPLICATION_CREDENTIALS
            value: /var/secrets/google/key.json
          - name: PROJECT_ID
            valueFrom:
              configMapKeyRef:
                name: cdmc-publisher-env-config
                key: PROJECT_ID
          - name: TOPIC_NAME
            valueFrom:
              configMapKeyRef:
                name: cdmc-publisher-env-config
                key: TOPIC_NAME
          - name: ADDRESS
            valueFrom:
              configMapKeyRef:
                name: cdmc-publisher-env-config
                key: ADDRESS
          - name: IF_NAME
            valueFrom:
              configMapKeyRef:
                name: cdmc-publisher-env-config
                key: IF_NAME
          - name: VERBOSE
            valueFrom:
              configMapKeyRef:
                name: cdmc-publisher-env-config
                key: VERBOSE
