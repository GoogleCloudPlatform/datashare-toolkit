---
apiVersion: v1
kind: Service
metadata:
  name: cdmc-publisher-service-lb
  labels:
    app: cdmc-publisher-service
spec:
  type: LoadBalancer
  ports:
    - protocol: UDP
      port: 50000
      name: multicast
      targetPort: 50000
  selector:
    app: cdmc-publisher-service
    version: v1
