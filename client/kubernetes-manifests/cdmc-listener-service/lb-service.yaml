---
apiVersion: v1
kind: Service
metadata:
  name: cdmc-listener-service-lb
  labels:
    app: cdmc-listener-service
spec:
  type: LoadBalancer
  ports:
    - protocol: UDP
      port: 50001
      name: multicast
      targetPort: 50001
  selector:
    app: cdmc-listener-service
    version: v1
